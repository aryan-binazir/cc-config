interface:
  display_name: "Rocket"
  short_description: "Move a configured tracker issue quickly into a reviewed PR"
  default_prompt: "Use $rocket with this configured tracker issue and optional codex or claude profile, branch, implementer, or grill modifiers to work from its configured authoritative checkout through configured critique, TDD implementation, commit and push, draft PR creation, and configured review."
