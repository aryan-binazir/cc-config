# Copy this file to lead.local.yaml and edit that. lead.local.yaml is ignored
# by git and wins over this file (deep merge). Workers are defined HERE and
# only here — name them anything (a role, a specialty), give each a
# description of what it is good at, and the agent picks by description.

workers:
  xhigh:
    description: The hardest problems — subtle bugs, architecture, gnarly debugging.
    runner: codex
    model: gpt-5.6-sol
    reasoning_effort: xhigh
    timeout_ms: 1800000
  high:
    description: Hard or subtle implementation work.
    runner: codex
    model: gpt-5.6-sol
    reasoning_effort: high
    timeout_ms: 1500000
  medium:
    description: Bulk implementation with a clear spec.
    runner: codex
    model: gpt-5.6-sol
    reasoning_effort: medium
    timeout_ms: 1500000
  low:
    description: Targeted, near-deterministic edits from an exact spec.
    runner: codex
    model: gpt-5.6-sol
    reasoning_effort: low
    timeout_ms: 1500000
  frontend:
    description: Frontend and UI work.
    runner: claude
    model: claude-opus-5
    reasoning_effort: high
    timeout_ms: 1500000
