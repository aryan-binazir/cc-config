interface:
  display_name: "Fast Rocket"
  short_description: "Move a Linear issue quickly into a reviewed PR"
  default_prompt: "Use $fast-rocket with this Linear issue and exact branch name to work from its authoritative worktree through a lightweight plan, TDD implementation, commit and push, PR creation, and Codex review."
