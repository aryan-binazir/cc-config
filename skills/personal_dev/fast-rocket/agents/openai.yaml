interface:
  display_name: "Fast Rocket"
  short_description: "Move a Linear issue quickly into a reviewed PR"
  default_prompt: "Use $fast-rocket with this Linear issue and optional exact branch name to work from its helper-returned authoritative worktree through a lightweight plan, TDD implementation, commit and push, PR creation, and Codex review."
