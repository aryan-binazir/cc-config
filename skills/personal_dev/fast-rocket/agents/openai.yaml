interface:
  display_name: "Fast Rocket"
  short_description: "Move a configured tracker issue quickly into a reviewed PR"
  default_prompt: "Use $fast-rocket with this configured tracker issue and optional branch, implementer, or grill modifiers to work from its authoritative worktree through configured critique, TDD implementation, commit and push, PR creation, and configured review."
